import styled from 'styled-components';

import { DialogPanel } from '../../lucid/DialogPanel';

const ProfileContainer = styled.div``;

export function ProfileModal() {
  return <DialogPanel>lol</DialogPanel>;
}
