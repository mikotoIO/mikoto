language: 'rust'
tasks:
  generate:
    command: cargo run --bin mikoto-codegen -- apps/superego/api.json crates/mikoto-client/src/generated.rs
    deps:
      - 'superego:generate'
    options:
      runFromWorkspaceRoot: true
